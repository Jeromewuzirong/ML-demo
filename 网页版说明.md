# 智能二分类建模平台 · 网页版使用说明

## 一、怎么启动

**最简单**：双击 `启动网页版.bat`（会自动装依赖、开浏览器）。

或手动：

```
pip install flask pandas scikit-learn openpyxl
python app.py
```

然后浏览器打开 http://127.0.0.1:5000

> 三个文件要放在同一个文件夹：`app.py`、`index.html`、`logistic_regression_auto.py`

## 二、Excel 模板

- **Sheet1**：训练数据（含目标列 Y，不能为空）
- **Sheet2**：待分类数据（可选；没有就只训练评估）
- 目标列 Y 为二值（0/1、是/否、Y/N 都行，会自动转 0/1）
- X（输入列）数量不限，会自动识别；也可在页面上手动增减

如果你的训练数据和待分类数据是两个独立文件，把它们分别复制成同一个 Excel 的
Sheet1、Sheet2 即可。

## 三、页面操作

1. **选择模式**：训练+分类 / 直接分类（用已保存模型）
2. **上传 Excel**：自动识别 X、Y 并填入“字段配置”
3. **字段配置**：点击可在“可选字段 / 输入字段”间移动；目标列可下拉修改
4. **预处理**：缺失值填充方式、是否标准化（都会真实生效）
5. **阈值**：拖动滑块，平衡精确率与召回率
6. **开始训练并分类**：显示交叉验证/单次切分性能、结果分布环形图、
   预测预览、特征重要性，并可“导出结果 Excel”

## 四、说明

- 训练完模型会自动保存（`_model.pkl`），之后可用“直接分类”模式对新数据快速打标。
- 交叉验证结果比单次切分更可信；样本越多越稳。
- 智能表头：会自动跳过 Excel 前面的说明/凡例行、处理合并单元格表头。
